# Leave empty in development to use the Vite proxy to http://localhost:3000
# For production builds, set the public API origin including /api path if needed:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
